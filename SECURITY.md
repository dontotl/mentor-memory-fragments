# 보안 및 공개 범위

이 프로젝트는 로컬 교육·멘토링 MVP입니다. 인터넷에 노출할 수 있는 운영 보안 수준을 보증하지 않습니다.

- 앱·음성 서비스는 기본적으로 `127.0.0.1`에서 실행합니다.
- 사진과 SQLite, 로컬 암호화 키는 `memory-app/data/`에 저장되며 Git에서 제외합니다. 사진 파일 자체는 암호화되지 않습니다.
- 인터뷰 글과 설정은 암호화하지만 서버와 키가 함께 유출되는 상황까지 방어하지 않습니다.
- 게스트 쿠키 소유권 구분은 가족 계정, 기기 간 로그인·복구를 대신하지 않습니다.
- 원음은 전사 중 임시 처리되며 서버에 지속 저장하지 않습니다.
- 외부 API는 설정·동의에 따라 명시적으로 호출합니다. API 키는 서버 설정에만 보관하세요.
- `.env`, DB, 개인 사진, 녹음, SSH 키, 모델·실행 로그를 이슈나 PR에 첨부하지 마세요.
- 결함 재현에는 가상 사진과 합성 문장만 사용하세요. 보안 취약점이나 인증정보는 공개 이슈에 올리지 마세요.

공개 저장소가 있다는 이유로 현재 앱 서버가 인터넷에 배포된 것은 아닙니다.
